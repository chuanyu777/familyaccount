-- V1 基线：代表「由 schema.sql 建出的初始库结构」。
--
-- 这个文件是空的、也是必须保持空的（只作为版本锚点）。
-- 首次启动时，迁移器会把 V1 直接标记为「已应用」而不执行它，
-- 因为无论是新库还是老库，schema.sql 都已经把 V1 这套表建好了。
--
-- 之后所有结构/数据变更，请新增 V2__xxx.sql、V3__xxx.sql……
-- 规则见 backend/MIGRATIONS.md
