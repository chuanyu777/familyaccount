
## 2026-09-14
- 用户本机没有 rsync（Windows Git Bash 不带），改用 python zipfile 打包 `family-ledger.zip`（99 文件 / 0.2 MB，含 backend/web/deploy + 根 package.json、lock、tsconfig、.dockerignore；排除 node_modules/target/dist/server/data/.git）
- 校验关键文件齐全（web/package.json 本就不存在，依赖在根 package.json）
- 指导用户用 scp（Windows 自带）或腾讯云控制台上传 zip
- 新增数据库版本化迁移：backend/src/main/java/com/familyledger/db/Migrator.java + resources/db/migration/V{n}__{desc}.sql，历史表 schema_migration（含 checksum，已应用脚本被改则拒绝启动）；V1 为空基线锚点，首次运行 adoptBaseline 只登记不执行
- 本地真实验证：已有数据的 MySQL 库重启后正确登记 V1、数据无损；临时 V2 探针脚本在 MySQL 上成功执行后已清理
- 新增 deploy/backup.sh（mysqldump 压缩、保留14天）、deploy/restore.sh（二次确认+先备份再导入）、backend/MIGRATIONS.md（加字段/删字段两步走规范）、README 维护章节
- 测试 13 全绿（新增 MigratorTest 4 个）
- 坑：Windows 写 .sh 必须保证 LF（已加 .gitattributes `*.sh text eol=lf`）；zip 不保留可执行位，服务器需 chmod +x
