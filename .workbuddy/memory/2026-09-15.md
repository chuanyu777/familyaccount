## 2026-09-15
- UI 改版定稿：方向 D 骨架（大圆角卡片/胶囊/泡泡）+ P2 燕麦焦糖（暖白 #FAF9F6、主色焦糖 #854F0B）+ 衬线标题/黑体正文/tabular 金额
- 附加范围：记一笔表单重设计（底部抽屉+自定义数字键盘）、底部导航加图标、Tab/弹层动效；本轮只出规范不改码
- 规范文档：docs/design-v2.md（令牌/组件/5页/动效/可访问性/与现有代码映射），待用户确认后实施

- UI v2 实施完成：base.css 全量换令牌（暖白 #FAF9F6 + 焦糖 #854F0B，圆角 10/14/20，按钮胶囊）；新增 StatHero/NumpadKeyboard/AppToast/toast.ts；TabBar 线性图标；App.vue 页面动效
- TransactionForm 重写为底部抽屉 + 自定义数字键盘（金额+分类即可保存）；记账页改结余卡/chips/日期胶囊/行卡/圆形FAB/骨架屏；资产页净资产卡+行卡；负债页总负债卡+还款进度条；分析页结余大卡
- 测试：Assets/Liabilities 的 statValue 断言改为 .hero__bubble / .hero__amount 结构；94 测试全绿，vue-tsc 零报错，build 145kB
- 坑：Docker Desktop 实际安装在 %LOCALAPPDATA%\Programs\DockerDesktop\Docker Desktop.exe（不在 Program Files）

## 用户反馈后的三处调整
- 记一笔：用户不要自绘数字键盘 → 删除 NumpadKeyboard.vue，改 inputmode=decimal 原生输入 + 输入清洗（最多 9 位整数 2 位小数）
- 金额溢出：StatBlock 值改 clamp(0.9375rem, 4.4vw, xl) + min-width:0 + ellipsis；分析页 .stats 网格改 minmax(0,1fr)；StatHero 大数 clamp(1.375rem,7vw,2xl) + overflow hidden
- 趋势图：去掉结余折线（效果差），改为柱下方 10px 结余小字，满万折算「万」，正负绿/红；图例只留收入/支出
